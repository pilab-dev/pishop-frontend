// This is a dummy file to resolve the build error.
// The original seed script is missing.

export const seed = async ({ payload, req }) => {
    payload.logger.info('Seeding database...');
    return;
}
